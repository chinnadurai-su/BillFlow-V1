// app.state.ts — Root NgRx state interface combining feature states.

// TODO: combine feature states as reducers are added, e.g.:
//   auth: AuthState          (from store/auth.reducer)
//   customers: CustomerState (from store/customer.reducer)
export interface AppState {}
