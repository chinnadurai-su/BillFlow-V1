// environment.prod.ts — Production environment settings for the BillFlow SPA.
export const environment = {
  production: true,
  // TODO: point at the production backend API base URL. Placeholder below.
  apiUrl: 'https://your-production-api.example.com/api',
};
