// environment.prod.ts — Production environment settings for the BillFlow SPA.
export const environment = {
  production: true,
  // TODO: point at the deployed backend (Render). Placeholder below.
  apiUrl: 'https://billflow-api.onrender.com/api',
};
