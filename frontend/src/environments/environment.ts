// environment.ts — Development environment settings for the BillFlow SPA.
export const environment = {
  production: false,
  // Backend REST API base URL (dev). Backend defaults to PORT 5000 (see ../.env.example).
  apiUrl: 'http://localhost:5000/api',
};
